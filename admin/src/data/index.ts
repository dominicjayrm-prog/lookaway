// This file intentionally left as a re-export.
// The full 91-object library with JSX render functions
// needs to be pulled from the local branch.
// For now, the admin builder uses the .ts data-only version.

export { objectLibrary, CATEGORIES, getObjectById, getObjectsByCategory } from './objectLibrary.ts';
